{
  "watch": ["src/server", "static"],
  "exec": "cross-env MODE=development node -r ./dotenv.config -r ts-node/register -r tsconfig-paths/register  src/server",
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"]
}
